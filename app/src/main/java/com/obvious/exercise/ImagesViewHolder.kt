package com.obvious.exercise

import android.content.Context
import android.view.View

class ImagesViewHolder(val mContext: Context, val itemView: View) : BaseViewHolder(itemView) {
    override fun bind(position: Int, baseListData: BaseListData) {

    }
}